import { Figtree, Meddon, Noto_Serif } from "next/font/google";

export const figtree = Figtree({ subsets: ["latin"] });
export const meddon = Meddon({ weight: "400", subsets: ["latin"] });
export const notoSerif = Noto_Serif({ subsets: ["latin"] });
